[project]
name = "biblia_offline"
version = "0.1.0"
dependencies = [
    "flet",
]

[tool.flet]
path = "main.py"
assets_dir = "assets"

# AGREGA ESTO PARA QUE EL APK NO NAZCA VACÍO
[tool.flet.android]
include_assets = ["assets/**"]

