[project]
name = "biblia_offline"
version = "0.1.0"
dependencies = ["flet"]

[tool.flet]
path = "main.py"
assets_dir = "assets"

[tool.flet.android]
# Especificamos el archivo para que no se le olvide al robot
include_assets = ["assets/Biblia.json"]


